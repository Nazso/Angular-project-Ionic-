import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'First demo ionic',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
